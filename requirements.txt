# Core GUI Framework (choose one)
PyQt6>=6.4.0
PyQt6-Fluent-Widgets[full]>=1.4.0
# PySide6>=6.4.0  # Alternative to PyQt6

# HTTP and async support
aiohttp>=3.8.0
requests>=2.28.0

# Text encoding detection
chardet>=5.0.0

# Translation engines (optional)
# deep-translator (removed)
# Alternative: libretranslate (simpler to install)
# libretranslate[cpu]>=1.3.0

# Optional: For better performance
uvloop>=0.17.0; sys_platform != "win32"  # Unix only

# Development dependencies (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=22.0.0
# flake8>=5.0.0
